ServerName localhost
ServerTokens Prod
ServerSignature Off

LoadModule proxy_module modules/mod_proxy.so
LoadModule proxy_http_module modules/mod_proxy_http.so
LoadModule proxy_wstunnel_module modules/mod_proxy_wstunnel.so
LoadModule rewrite_module modules/mod_rewrite.so
LoadModule deflate_module modules/mod_deflate.so
LoadModule headers_module modules/mod_headers.so
LoadModule expires_module modules/mod_expires.so
LoadModule ssl_module modules/mod_ssl.so
LoadModule socache_shmcb_module modules/mod_socache_shmcb.so

Listen 80

# Step 1 Apache config — HTTP only, used BEFORE SSL cert is issued.
# After running certbot, uncomment the HTTPS block below.

<VirtualHost *:80>
    # Let certbot verify domain ownership
    Alias /.well-known/acme-challenge/ "/var/www/certbot/.well-known/acme-challenge/"
    <Directory "/var/www/certbot/.well-known/acme-challenge/">
        Options None
        AllowOverride None
        Require all granted
    </Directory>

    # Redirect everything else to HTTPS
    RewriteEngine On
    RewriteCond %{REQUEST_URI} !^/\.well-known/acme-challenge/
    RewriteRule ^ https://%{HTTP_HOST}%{REQUEST_URI} [R=301,L]
</VirtualHost>

# ── HTTPS ─────────────────────────────────────────────────────────────────────
# Uncomment this block AFTER you have run certbot and received the certificate.
# Replace YOUR_DOMAIN with your actual domain (e.g. dashboard.yourcompany.de)
#
# Listen 443
# <VirtualHost *:443>
#     ServerName YOUR_DOMAIN
#
#     SSLEngine on
#     SSLCertificateFile    /etc/letsencrypt/live/YOUR_DOMAIN/fullchain.pem
#     SSLCertificateKeyFile /etc/letsencrypt/live/YOUR_DOMAIN/privkey.pem
#     Include /etc/letsencrypt/options-ssl-apache.conf
#
#     # Security headers
#     Header always set X-Frame-Options "DENY"
#     Header always set X-Content-Type-Options "nosniff"
#     Header always set X-XSS-Protection "1; mode=block"
#     Header always set Referrer-Policy "strict-origin-when-cross-origin"
#     Header always set Strict-Transport-Security "max-age=31536000; includeSubDomains"
#     Header always set Content-Security-Policy "default-src 'self'; img-src 'self' data: https:; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline' https:; connect-src 'self' https: wss:; font-src 'self' data: https:; frame-ancestors 'none'; base-uri 'self'; form-action 'self'"
#
#     # Compression
#     AddOutputFilterByType DEFLATE text/plain text/css application/json application/javascript text/javascript
#
#     ProxyPreserveHost On
#     RequestHeader set X-Forwarded-Proto "https"
#
#     # API — proxy to NestJS backend
#     ProxyPass        /api/ http://nestjs-api:3001/api/ connectiontimeout=5 timeout=60
#     ProxyPassReverse /api/ http://nestjs-api:3001/api/
#
#     # Static assets — cache aggressively (content-hashed filenames)
#     <LocationMatch "\.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2)$">
#         ExpiresActive On
#         ExpiresDefault "access plus 1 year"
#         Header set Cache-Control "public, immutable"
#     </LocationMatch>
#
#     # WebSocket support (Next.js)
#     RewriteEngine On
#     RewriteCond %{HTTP:Upgrade} websocket [NC]
#     RewriteCond %{HTTP:Connection} upgrade [NC]
#     RewriteRule ^/?(.*) "ws://nextjs-frontend:3000/$1" [P,L]
#
#     # Frontend — proxy to Next.js
#     ProxyPass        / http://nextjs-frontend:3000/ connectiontimeout=5 timeout=60
#     ProxyPassReverse / http://nextjs-frontend:3000/
#
#     # No caching on HTML pages
#     <LocationMatch "\.html$">
#         Header set Cache-Control "no-cache, no-store, must-revalidate"
#         Header set Pragma "no-cache"
#         Header set Expires "0"
#     </LocationMatch>
# </VirtualHost>
